-- Supabase seed file for Attic
-- Seed data will be added in Task 0.4 (Database Migrations)

-- Enable required extensions (also done in migrations, but safe to run here)
CREATE EXTENSION IF NOT EXISTS vector;
CREATE EXTENSION IF NOT EXISTS pg_trgm;
