# Task 0.1: Backend Project Scaffolding

## 0) Outcome

Developers can run a FastAPI backend server locally with pytest tests, ruff linting, and proper Python 3.12 project structure. The backend foundation supports async operations and is ready for feature development.

## 1) Scope

### In-scope
- [ ] Create `src/backend/` directory structure following FastAPI best practices
- [ ] Set up `pyproject.toml` with Python 3.12, FastAPI, SQLAlchemy 2.0, pytest, ruff dependencies
- [ ] Create main FastAPI application entry point with health check endpoint
- [ ] Configure ruff for linting and formatting
- [ ] Set up pytest with async support and basic configuration
- [ ] Create initial directory structure: `app/`, `tests/`, `alembic/`
- [ ] Add `.gitignore` for Python artifacts
- [ ] Create `README.md` with setup instructions

### Out-of-scope / Non-goals
- Database connection setup (Task 0.3, 0.4)
- Alembic migrations configuration (Task 0.4)
- Authentication middleware (Epic 1)
- Any API endpoints beyond health check
- Docker configuration (Task 0.7)
- CI/CD pipeline (Task 0.6)

## 2) System context

### Components touched
- **Next.js route(s)**: None
- **FastAPI endpoint(s)**: `GET /health` - Health check endpoint
- **DB tables/columns**: None
- **Step Functions state**: None
- **Lambda function(s)**: None
- **Third parties**: None

### Files to create
- `src/backend/pyproject.toml` - Project configuration and dependencies
- `src/backend/app/__init__.py` - Package init
- `src/backend/app/main.py` - FastAPI application entry point
- `src/backend/app/routers/__init__.py` - Routers package
- `src/backend/app/models/__init__.py` - Pydantic models package
- `src/backend/app/services/__init__.py` - Business logic package
- `src/backend/app/repositories/__init__.py` - Data access package
- `src/backend/tests/__init__.py` - Tests package
- `src/backend/tests/conftest.py` - Pytest fixtures
- `src/backend/tests/test_health.py` - Health check tests
- `src/backend/ruff.toml` - Ruff configuration
- `src/backend/.gitignore` - Python gitignore
- `src/backend/README.md` - Backend setup documentation

### Invariants (must always be true)
- All Python code must pass ruff linting
- All tests must pass before merging
- Type hints required on all public functions

## 3) API contracts (authoritative)

### Request/Response

```python
# Health check endpoint - no request body
from pydantic import BaseModel

class HealthResponse(BaseModel):
    status: str  # "healthy"
    version: str  # e.g., "0.1.0"
```

### Status codes
- `200 OK` - Server is healthy

### Pagination rules
N/A - Health check only

### Auth requirements
None - Health check is public

## 4) Data model changes

N/A - No database changes in this task. Database setup is in Task 0.3 and 0.4.

## 5) Workflow & state machine

N/A - Not a workflow task.

## 6) Implementation plan (ordered)

1. Create `src/backend/` directory
2. Create `pyproject.toml` with dependencies:
   - Python 3.12
   - fastapi[standard]
   - sqlalchemy[asyncio] >= 2.0
   - pydantic >= 2.0
   - pytest
   - pytest-asyncio
   - httpx (for testing)
   - ruff
3. Create `ruff.toml` with configuration:
   - Line length: 100
   - Target Python 3.12
   - Enable standard rules (E, F, I, UP)
4. Create directory structure (`app/`, `tests/`)
5. Create `app/main.py` with FastAPI app and health endpoint
6. Create `app/models/__init__.py` with HealthResponse model
7. Create `tests/conftest.py` with async test client fixture
8. Create `tests/test_health.py` with health endpoint test
9. Verify: `ruff check .` passes
10. Verify: `ruff format .` makes no changes
11. Verify: `pytest tests/ -v` passes

## 7) Observability & analytics

### Logs (required fields)
- Application startup: `{"event": "server_started", "version": "X.X.X"}`
- Health check requests logged at DEBUG level only

### Metrics
- `http_requests_total` - Count of requests (future, not in this task)

### Sentry tags/breadcrumbs
N/A - Sentry integration is in Observability tasks (O.1)

### PostHog events
N/A - PostHog integration is in Observability tasks (O.2)

## 8) Security & privacy checklist

- [ ] Data minimization checks - N/A (no user data)
- [ ] Secrets handling - N/A (no secrets yet, but structure supports env vars)
- [ ] RLS verification - N/A (no database)
- [ ] Third-party payload review - N/A (no third parties)
- [ ] PII-safe logging - N/A (no user data logged)
- [ ] Server-side auth - N/A (health check is public)
- [ ] Input validation - N/A (no user input)

## 9) Test plan

### Unit tests
- [ ] `test_health_endpoint_returns_200` - GET /health returns 200 status
- [ ] `test_health_endpoint_returns_healthy_status` - Response contains status="healthy"
- [ ] `test_health_endpoint_returns_version` - Response contains version string

### Integration tests
N/A - No external integrations in this task

### E2E (Playwright)
N/A - No UI

## 10) Acceptance criteria (binary)

- [ ] `src/backend/` directory exists with proper structure
- [ ] `pyproject.toml` has all required dependencies
- [ ] `python -m pytest tests/ -v` passes
- [ ] `ruff check .` passes with no errors
- [ ] `ruff format --check .` passes (code is formatted)
- [ ] `GET /health` returns `{"status": "healthy", "version": "X.X.X"}`
- [ ] README.md contains setup instructions that work

## 11) Rollout

### Feature flags
None - Infrastructure task

### Backward compatibility
N/A - New project, no existing code

### Migration order
1. Create directory structure
2. Add configuration files
3. Add application code
4. Add tests

### Rollback plan
Delete `src/backend/` directory if needed (no dependencies yet)

---

## Progress Tracking

**Status**: NOT_STARTED
**Last Updated**: 2026-01-28

### Completed
- {Nothing yet}

### Remaining
- All acceptance criteria above

### Blocked By
- {Nothing}

### Implementation Notes
- {Filled during implementation}
