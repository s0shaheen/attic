# Task 1.5: Sign Out Functionality

## 0) Outcome

Users can sign out of Attic, which clears their session, invalidates tokens, and redirects them to the login page. Sign out is accessible from the navigation/header and works reliably.

## 1) Scope

### In-scope
- [ ] Create sign out action/function
- [ ] Add sign out button to user menu/header
- [ ] Clear session cookies via Supabase Auth
- [ ] Redirect to login page after sign out
- [ ] Handle sign out errors gracefully
- [ ] Create user menu dropdown component (if not exists)

### Out-of-scope / Non-goals
- Backend token invalidation (Supabase handles this)
- "Sign out all devices" functionality
- Session timeout/auto-logout
- Account deletion (Task 1.6)
- Full navigation/header design (just add sign out button)

## 2) System context

### Components touched
- **Next.js route(s)**: `app/auth/signout/route.ts` (optional server action)
- **FastAPI endpoint(s)**: None (Supabase handles sign out)
- **DB tables/columns**: None (session management only)
- **Step Functions state**: None
- **Lambda function(s)**: None
- **Third parties**: Supabase Auth

### Files to create/update
- `src/frontend/components/layout/UserMenu.tsx` - User menu with sign out (create or update)
- `src/frontend/lib/supabase/auth.ts` - Add signOut function (update)
- `src/frontend/app/auth/signout/route.ts` - Optional server-side sign out route

### Invariants (must always be true)
- Sign out must clear ALL session data (cookies, local state)
- After sign out, protected routes must not be accessible
- Sign out must work even if network is slow
- User must be redirected to login after sign out

## 3) API contracts (authoritative)

### Supabase Auth Method

```typescript
// Sign out the current user
const { error } = await supabase.auth.signOut();
```

### Server-Side Sign Out Route (Optional but recommended)

```typescript
// app/auth/signout/route.ts
import { createClient } from '@/lib/supabase/server';
import { NextResponse } from 'next/server';

export async function POST(request: Request) {
  const supabase = await createClient();
  const { error } = await supabase.auth.signOut();

  if (error) {
    // Log error but still redirect to login
    console.error('Sign out error:', error.message);
  }

  // Redirect to login
  return NextResponse.redirect(new URL('/auth/login', request.url));
}
```

### TypeScript Types

```typescript
// Sign out function type
type SignOutFn = () => Promise<{ error: Error | null }>;
```

## 4) Data model changes

N/A - No database changes. Sign out only affects session state.

## 5) Workflow & state machine

### Sign Out Flow
1. User clicks "Sign Out" button
2. Show loading/confirmation state
3. Call `supabase.auth.signOut()`
4. Supabase clears cookies
5. Clear any local state (React context, cache)
6. Redirect to `/auth/login`

### Error Handling
- Network error → Still clear local state, redirect to login
- Supabase error → Log error, still clear local state and redirect
- User should always end up signed out, even if backend call fails

## 6) Implementation plan (ordered)

1. **Add signOut utility function**
   - Update `lib/supabase/auth.ts`
   - Export `signOut()` function
   - Handle errors gracefully

2. **Create server-side sign out route (optional)**
   - `app/auth/signout/route.ts`
   - Clear session server-side
   - Redirect to login

3. **Create UserMenu component**
   - Use shadcn/ui DropdownMenu
   - Show user avatar/name
   - Include Sign Out item
   - Handle loading state

4. **Integrate with layout/header**
   - Add UserMenu to header component
   - Only show when authenticated
   - Show login button when not authenticated

5. **Handle client-side sign out**
   - Clear TanStack Query cache
   - Reset any local state
   - Navigate to login

6. **Write tests**
   - Test sign out function
   - Test redirect behavior
   - Test error handling

## 7) Observability & analytics

### Logs (required fields)
- Sign out events: `{ event: "user_signed_out", user_id }` (server-side)
- Sign out errors: `{ event: "signout_error", error_message }` (no PII)

### Metrics
- Sign out rate (how often users sign out)
- Sign out errors (should be rare)

### Sentry tags/breadcrumbs
- Breadcrumb: "user_signed_out"
- Error: Sign out failures (log error type, not user data)

### PostHog events
- `user_signed_out` - User completed sign out
- `signout_error` - Sign out failed: `{ error_type: "..." }`

## 8) Security & privacy checklist

- [ ] Data minimization checks - N/A
- [ ] Secrets handling - N/A
- [ ] RLS verification - N/A
- [ ] Third-party payload review - N/A
- [ ] PII-safe logging - Only log user_id, never email
- [ ] Server-side auth - Server route validates session before sign out
- [ ] Input validation - N/A

### Session Security
- [ ] HTTP-only cookies cleared
- [ ] No session data remains in localStorage
- [ ] React Query cache cleared
- [ ] Auth context reset

## 9) Test plan

### Unit tests
- [ ] `test_signOut_calls_supabase_signout` - Function calls Supabase
- [ ] `test_signOut_returns_no_error_on_success` - Success case
- [ ] `test_signOut_handles_error_gracefully` - Error case
- [ ] `test_UserMenu_renders_user_info` - Shows user name/avatar
- [ ] `test_UserMenu_has_signout_button` - Sign out option visible

### Integration tests
- [ ] `test_signout_route_clears_session` - Server route works
- [ ] `test_signout_route_redirects_to_login` - Redirects correctly
- [ ] `test_signout_clears_react_query_cache` - Cache cleared

### E2E (Playwright)
- [ ] `test_signout_flow_complete` - Full sign out flow
- [ ] `test_after_signout_protected_route_redirects` - Cannot access protected routes
- [ ] `test_signout_button_visible_when_authenticated` - Button shows for auth users

## 10) Acceptance criteria (binary)

- [ ] Sign out function exists and calls Supabase Auth
- [ ] UserMenu component exists with sign out option
- [ ] Sign out button visible in header/navigation for authenticated users
- [ ] Clicking sign out clears session
- [ ] User redirected to `/auth/login` after sign out
- [ ] Protected routes inaccessible after sign out
- [ ] Sign out works even if Supabase call fails (graceful degradation)
- [ ] Loading state shown during sign out
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] All tests pass

## 11) Rollout

### Feature flags
None - Core auth functionality

### Backward compatibility
N/A - New feature

### Migration order
1. Add signOut utility
2. Create server route (optional)
3. Create UserMenu component
4. Integrate with header/layout
5. Test full flow

### Rollback plan
1. Sign out is critical - if broken, users can't switch accounts
2. Fallback: users can clear cookies manually
3. Fix immediately if issues found

---

## Progress Tracking

**Status**: NOT_STARTED
**Last Updated**: 2026-01-28
**Merged**: No
**Branch**: feature/1-1.5-signout

### Completed
- {Nothing yet}

### Remaining
- All acceptance criteria above

### Blocked By
- Task 1.2 (Session management)
- Task 1.4 (Login page - for redirect destination)

### Implementation Notes
- {Filled during implementation}
